<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="mapper.packageProduct">
	<insert id="insertPackageProduct" parameterType="packageProduct" useGeneratedKeys="true" keyProperty="packageNum">
		insert into package (sellerNum, cateNum, packageName, content, packagePrice, stock, startDate, endDate, packageUnit) 
		values (#{sellerNum}, #{cateNum}, #{packageName}, #{content}, #{packagePrice}, #{stock}, #{startDate}, #{endDate}, #{packageUnit})
	</insert>
</mapper>